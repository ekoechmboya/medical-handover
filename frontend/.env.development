# URL of the Django backend that serves the analysis API.
# Default target is the deployed Render backend; uncomment the second line to
# point at a local Django instance instead.
NEXT_PUBLIC_API_BASE_URL=https://medical-handover.onrender.com
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000